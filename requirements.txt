discord.py>=2.0
# 2.3+ includes ClientTransaction fixes for X front-end changes ("Couldn't get KEY_BYTE indices").
twikit>=2.3.3
# Replaces httpx transport with curl-impersonate to bypass Cloudflare JA3/TLS fingerprint blocks.
httpx-curl-cffi>=0.1.5
# Scweet v5 — actively maintained X scraper with native curl_cffi (replaces twikit for reads).
Scweet>=5.3
python-dotenv
openai
aiohttp
requests
web3
base58
# Keep compatible with Windows installs that lack script-write perms
websockets>=11.0.3,<12
fastapi
uvicorn[standard]
python-multipart
telethon
psycopg[binary]
